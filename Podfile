# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GuruSearch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GuruSearch

  target 'GuruSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GuruSearchUITests' do
    # Pods for testing
  end

end

# cocoapods-keys
plugin 'cocoapods-keys', {
  :project => "GuruSearch",
  :keys => [
    "GuruNaviAPIKeyId"
]}
